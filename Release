-- Configs --

local buff = false --if this is true it will say /buff in chat activating all buffs you have--

local AutoPromote = false -- If true whenever you join a game then it will see if you can promote and it will promote --

local FPSBoost = false -- if on it will boost your fps
---------------------------------------
loadstring(game:HttpGet("https://raw.githubusercontent.com/RadiantSkullz/BetterAutoBoss/refs/heads/main/Insomnia"))()
